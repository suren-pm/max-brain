# Max Brain Server — Railway deployment (Pipecat streaming architecture)
# Stack: Meeting BaaS → Pipecat (Streaming STT → Claude → Streaming TTS) → Meeting BaaS

# Pipecat framework with required service integrations
pipecat-ai[anthropic,google,deepgram,silero,websocket]==0.0.108

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# HTTP client (Jira API, Meeting BaaS API)
httpx>=0.28.0

# Protobuf (for Pipecat frame serialization between MBaaS↔Pipecat WebSockets)
protobuf>=5.27.0

# Environment
python-dotenv>=1.0.0

# Logging
loguru>=0.7.0
